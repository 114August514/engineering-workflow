# {{PROJECT_NAME}}

> 写法见 skill 的 `references/intake.md`。这份文件是验收标准的唯一事实源。
> 每条标准的 `REQ-ID` 必须出现在对应测试的名字里，`make audit` 会做双向检查。

## 问题

<两三句：现在什么情况、什么地方不成立、为什么值得解决。不要写技术方案。>

## 用户与场景

<谁、在什么时候、为了什么用它>

## 成功判定

<上线后看什么现象/指标，就说明这事做成了>

## 非目标

> **这一节不许为空。至少五条。** 它是用来挡住 AI 和你自己的护栏。

- 不做 ……（若将来要做：从 …… 改起）
- 不做 ……
- 不做 ……
- 不做 ……
- 不做 ……

## 验收标准

> EARS 五句式，每条配一个**具体**例子（含数字或具体输入输出）。
> 每三条正常路径至少配一条"如果……那么……"。

### <模块名>

- **REQ-XXX-001** 当 `<触发>` 时，系统应当 `<响应>`
  - 例：……

- **REQ-XXX-002** 如果 `<异常>`，那么系统应当 `<响应>`
  - 例：……

## 约束

<部署环境、必须兼容的东西、合规要求、预算、时间>

## 未决问题

> **进 P2 之前必须清空。** 挂着的问题到实现阶段会被 AI 随手替你决定。

- [ ] …… （谁：…… 什么时候之前定：……）
