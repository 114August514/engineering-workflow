# 领域词汇与边界

> 写法见 skill 的 `references/domain.md`。
> **这里定下的名字是代码、数据库、接口、日志、文档的唯一事实源。**

## 术语

| 术语 | 代码标识 | 定义 | 不是什么 |
|---|---|---|---|
| | | | |

> "不是什么"这一列不能省——定义靠划边界才清楚。

## 限界上下文

### <上下文名>

- 职责：<一句话>
- 拥有的数据：
- 对外提供：
- 依赖：<上游 / 下游>

## 上下文关系

<谁调谁、同步还是异步、谁是上游。依赖方向必须是单向的，有环就要重新划。>

## 不变量

> 任何时候都必须成立的断言。每条至少落到三个地方：数据库约束、代码断言、
> 一条尝试违反它并断言失败的测试。

- **INV-001** ……
- **INV-002** ……

## 核心状态机

### <实体名>

| 当前状态 | 事件 | 新状态 | 条件 |
|---|---|---|---|
| | | | |

> **表里没列出的组合全部非法**，每个非法组合都是一条测试：
> 调用它，断言被拒绝且状态没变。
